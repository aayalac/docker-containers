# Usos para docker compose

Se pueden ejecutar desde el CLI, ya sea de Windows o Linux:
---
- Saber versión actual de Docker Compose
```bash
docker-compose --version
```
- Construir con Docker Compose
```bash
docker-compose up -d --build
```
- Eliminar compose 
```bash
docker-compose down
```